spring.application.name=shop

# Thymeleaf config
spring.thymeleaf.check-template-location=false

# Logging
debug=true

# Multipart file upload
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=100MB
spring.servlet.multipart.max-request-size=400MB

# Spring config
spring.main.allow-circular-references=true

# Active profiles
# Use 'memory' for in-memory repositories, 'dev' for H2 database, 'prod' for MySQL
spring.profiles.active=dev

# Database config - H2 for development
spring.datasource.url=jdbc:h2:mem:shopdb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# JPA/Hibernate config
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# MySQL settings for production (uncomment and configure for production)
# spring.datasource.url=jdbc:mysql://localhost:3306/shopdb?useSSL=false&serverTimezone=UTC
# spring.datasource.username=root
# spring.datasource.password=your_password
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect